"""Graph traversal — the 100%-transitive-resolution metric lives here (Phase 3).

Stub — implemented in Phase 3, Section 6.2: ``descendants_of``,
``paths_to_vulnerable`` (all simple paths), and ``is_on_path_to_vulnerable``.
"""

from __future__ import annotations
